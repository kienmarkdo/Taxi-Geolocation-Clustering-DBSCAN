This version produces ONE SINGLE CSV file that contains ALL CLUSTERS.
The following data is outputted:

Cluster ID (starting at 1), longitude (average of all starting longitudes of all TripRecords in this cluster), latitude (same as longitude), number of points in the cluster.

The output files are named according to their EPS and MIN_PTS values and are sorted from largest cluster to smallest cluster.